# MetaView — News Intelligence Backend

This repo contains:
- Notebook (experiments & pipeline prototyping)
- FastAPI backend (production-ready entrypoint)

## Run locally
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8080
